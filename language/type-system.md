# Type System Overview

Ved uses a statically analyzable type system.

Goals of the type system:

- enforce authority constraints
- track side-effect capability
- ensure state schema integrity
- support long-lived evolution

Potential future features:

- authority types
- effect capability types
- persistent schema versioning
- domain isolation guarantees
