# Runtime Determinism

Runtime determinism requires:

- controlled scheduling
- explicit input ordering
- isolation from system timing variability

Non-deterministic inputs must be modeled as events.
