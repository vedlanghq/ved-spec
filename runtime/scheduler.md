# Scheduler Model

Scheduler selects transition slices for execution.

Responsibilities:

- fairness across domains
- responsiveness under load
- convergence prioritization

Scheduler must remain deterministic given identical inputs.
